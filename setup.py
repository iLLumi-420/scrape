from setuptools import setup

setup(
    name = 'scrape',
    version = '1.0.0',
    description = 'Project to scrape price and title from daraz and parse the unit from the title',
    author='Sandeep Parajuli',
    packages=['scrape'],
)